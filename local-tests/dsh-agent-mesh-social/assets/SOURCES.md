# Asset provenance

- `dsh-home.png` ← local DSH web preview; workspace and session labels were replaced with fictional demo values before capture.
- `dsh-model-menu.png` ← local DSH web preview; workspace and session labels were replaced with fictional demo values before capture.
